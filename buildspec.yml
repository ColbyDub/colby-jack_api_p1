version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - cd registrar
      - mvn install
  post_build:
    commands:
      - echo build completed on `date`
artifacts:
  files:
    - registrar/target/registrar-1.0.SNAPSHOT.war
